import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'An Inspector Calls Extract Walkthrough | GCSE Practice',
  description:
    "Walk through the Inspector's final speech from Act 3 line by line: what to notice, the AO2 analysis to write and links to Priestley's message for GCSE exams.",
  alternates: { canonical: '/revision/texts/an-inspector-calls/extract-walkthrough' },
}

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
