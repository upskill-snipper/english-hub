// ─── Shared pricing copy strings ──────────────────────────────────────
//
// Consumer-facing disclosures that sit alongside displayed prices. Kept
// centralised so legal-sensitive wording is updated in one place.
//
// VAT_LABEL: Required under the UK Price Marking Order 2004 (Art. 5) so
// that the "selling price" shown to consumers makes the VAT position
// explicit. The English Hub is not currently VAT-registered, so prices
// are shown with no VAT added. When the business crosses the UK VAT
// threshold, flip this line to "Prices include VAT at 20%" (or the
// applicable rate) before charging VAT-inclusive amounts.

export const VAT_LABEL =
  'Prices shown in GBP. The English Hub is not currently VAT-registered, so no VAT is added to the advertised price.'
