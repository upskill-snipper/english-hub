node_modules/
.next/
out/
dist/
.env
.env.local
.env*.local
.env.production
.env.production.*
.env.staging
*.tsbuildinfo
next-env.d.ts
.vercel
.DS_Store
*.log
coverage/
src/data/temp/

# Business documents
docs/Biz Files/
*.docx
*.xlsx
*.pdf
~$*

# Backup and temp files
*.bak
*.bak-seo-autofix
*.bak-seo-fix
*.bak-seo-titlededup
POETRY_ANTHOLOGY_SUMMARY.txt

# Generated Word documents
docs-word/

# Playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/

# Apple / Google credentials (NEVER commit)
secrets/
*.p8
*.p12
*.mobileprovision
google-play-service-account.json

# External audit pack (reference only, not committed)
external-audit-pack/

# Agent-fleet artifacts (D:/AI-Agent-Server) — never commit to the site repo.
__pycache__/
*.pyc
/*.py

# Operator scratch space - dumps and one-off analysis scripts, never shipped.
scratch/

# Linocut comic plates, one SVG per registered piece, rendered from
# src/data/comics by scripts/generate-comic-plates.mjs in prebuild, predev and
# pretest. Build output: the drawings' source is the TSX, and the manifest that
# names these files (src/lib/comics/plates.generated.ts) is committed.
/public/comics/
