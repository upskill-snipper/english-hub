# The rendered PDFs in dist/ are deliverables (also copied to the desktop), so we
# DO track them — even though the repo-root .gitignore excludes `dist/` globally.
# Re-include this dist dir and the PDFs inside it; ignore only the scratch HTML.
!dist/
!dist/*.pdf
__build_*
