// Entry point is handled by expo-router via "main": "expo-router/entry" in package.json.
// This file is kept as a convenience re-export so tooling that scans for App.tsx still works.
import 'expo-router/entry';
export { default } from 'expo-router/entry';
